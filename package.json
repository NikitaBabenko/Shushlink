{
  "name": "shushlink",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Zero-backend, zero-knowledge secret sharing. The secret lives in the URL fragment; the server never sees it.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "hash-wasm": "^4.11.0",
    "qrcode-generator": "^1.4.4"
  },
  "devDependencies": {
    "png-to-ico": "^2.1.8",
    "sharp": "^0.33.5",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.5"
  }
}
